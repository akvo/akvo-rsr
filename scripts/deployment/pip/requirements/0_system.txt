# Build with either env64 or envintel - see setup/dev/dev_env_setup_for_osx.txt for details

# Requirements for installing packages from Mercurial repositories
mercurial==1.8.4

# Requirements for building supervisor
meld3==0.6.7

# System modules -- these would normally be installed outside a virtualenv
virtualenv==1.6.1
virtualenvwrapper==2.7.1
supervisor==3.0a10 # currently only operates outside virtualenv
